launch:
	A wrapper for launching any shell-launchable application.
	Its purpose is to launch the window of the application on the same tag in the suckless Dynamic Window Manager (dwm)

clock:
	A simple clock that updates every 60 seconds and so updates the `xsetroot -name` of dwm.

wit:
	A directory descriptor

find-cursor:
	This one was not written by me but I compiled it & linkes it with xbindkeys

toggle_left:
	Toggles the left-handed feature of the mouse
